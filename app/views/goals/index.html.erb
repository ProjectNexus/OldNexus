<h1>Listing goals</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Timeline</th>
    <th>Goalable</th>
    <th>Golable type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @goals.each do |goal| %>
  <tr>
    <td><%= goal.name %></td>
    <td><%= goal.description %></td>
    <td><%= goal.timeline %></td>
    <td><%= goal.goalable_id %></td>
    <td><%= goal.golable_type %></td>
    <td><%= link_to 'Show', goal %></td>
    <td><%= link_to 'Edit', edit_goal_path(goal) %></td>
    <td><%= link_to 'Destroy', goal, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Goal', new_goal_path %>
